{
  size: {
    // scaleFactor: 1,
    // NOTE: this needs to be split-up or reconsidered in some way — t-shirt
    // sizes don't make sense without context
    // ALSO: should spacing scale with the platform?
    // space: {
    //   xsmall: { value: "{size.scale.25.value}" }, // 2px between internal elements
    //   small: { value: "{size.scale.50.value}" }, // 4px between internal elements
    //   regular: { value: "{size.scale.100.value}" }, // 8px between internal elements - base
    //   medium: { value: "{size.scale.150.value}" }, // 12px internal gutters
    //   large: { value: "{size.scale.200.value}" }, // 16px between external elements - base
    //   xlarge: { value: "{size.scale.300.value}" }, // 24px between external elements - medium
    //   xxlarge: { value: "{size.scale.400.value}" }, // 32px between external elements - large
    // },
  },
}
