{
  size: {
    // scaleFactor: 1.25,
    // NOTE: this needs to be split-up or reconsidered in some way — t-shirt
    // sizes don't make sense without context
    // ALSO: should spacing scale with the platform?
    // space: {
    //   xsmall: { value: "{size.scale.25.value}" },
    //   small: { value: "{size.scale.50.value}" },
    //   regular: { value: "{size.scale.100.value}" },
    //   medium: { value: "{size.scale.150.value}" },
    //   large: { value: "{size.scale.200.value}" },
    //   xlarge: { value: "{size.scale.300.value}" },
    //   xxlarge: { value: "{size.scale.400.value}" },
    // },
  },
}
