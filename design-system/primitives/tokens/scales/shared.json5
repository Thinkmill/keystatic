{
  size: {
    alias: {
      singleLineHeight: { value: "{size.scale.400.value}", isShared: true },
      singleLineWidth: { value: "{size.scale.2400.value}", isShared: true },
    },
    element: {
      xsmall: { value: "{size.scale.200.value}", isShared: true },
      small: { value: "{size.scale.300.value}", isShared: true },
      regular: { value: "{size.scale.400.value}", isShared: true }, // base size
      medium: { value: "{size.scale.450.value}", isShared: true },
      large: { value: "{size.scale.600.value}", isShared: true },
      xlarge: { value: "{size.scale.800.value}", isShared: true },
    },
    icon: {
      small: { value: "{size.scale.150.value}", isShared: true },
      regular: { value: "{size.scale.200.value}", isShared: true }, // base size
      medium: { value: "{size.scale.300.value}", isShared: true },
      large: { value: "{size.scale.400.value}", isShared: true },
    },
    shadow: {
      small: {
        value: "0 {size.scale.10.value} {size.scale.25.value} 0",
        isShared: true,
      },
      medium: {
        value: "0 {size.scale.40.value} {size.scale.75.value} 0",
        isShared: true,
      },
      large: {
        value: "0 {size.scale.100.value} {size.scale.300.value} 0",
        isShared: true,
      },
    },
    // NOTE: this needs to be split-up or reconsidered in some way — t-shirt
    // sizes don't make sense without context
    // ALSO: should spacing scale with the platform?
    space: {
      xsmall: { value: "{size.scale.25.value}", isShared: true }, // 2px between internal elements
      small: { value: "{size.scale.50.value}", isShared: true }, // 4px between internal elements
      regular: { value: "{size.scale.100.value}", isShared: true }, // 8px between internal elements - base
      medium: { value: "{size.scale.150.value}", isShared: true }, // 12px internal gutters
      large: { value: "{size.scale.200.value}", isShared: true }, // 16px between external elements - base
      xlarge: { value: "{size.scale.300.value}", isShared: true }, // 24px between external elements - medium
      xxlarge: { value: "{size.scale.400.value}", isShared: true }, // 32px between external elements - large
    },
  },
}
