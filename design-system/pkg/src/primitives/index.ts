// constrain "@keystar/ui" to "@keystar/primitives", without publishing
export const TOKEN_PREFIX = 'kui';

export const THEME_DEFAULT = `${TOKEN_PREFIX}-theme`;
export const SCHEME_AUTO = `${TOKEN_PREFIX}-scheme--auto`;
export const SCHEME_LIGHT = `${TOKEN_PREFIX}-scheme--light`;
export const SCHEME_DARK = `${TOKEN_PREFIX}-scheme--dark`;
