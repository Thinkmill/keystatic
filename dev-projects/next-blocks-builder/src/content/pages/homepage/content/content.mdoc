I would love a way to have a nested `document` field as a component block.

Something with different formatting tools, and I can wrap in a `prose` class.

Looks like the UI works great, but it won't persist on save.
