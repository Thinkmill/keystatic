---
title: URL field
---
The `url` field stores a string that is a sanitized URL. It uses `@braintree/sanitize-url` under the hood.

## Usage example

```typescript
url: fields.url({
  label: 'URL',
  description: 'The website URL'
})
```

## Type signature

```typescript
url({
  label: string;
  defaultValue?: string;
  validation?: {
    isRequired?: IsRequired;
  };
  description?: string;
})
```
